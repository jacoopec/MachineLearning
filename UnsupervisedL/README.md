L’obiettivo dell’unsupervised learning `e trovare pattern nascosti in dati non etichettati.

Si tratta di raggruppare ossia si creano dei gruppi (o classi) di dati che sono simili e in particolare devono avere
due caratteristiche:
• alta similarit`a intraclasse
• bassa similarit`a interclasse

Il problema `e che la similarit`a non `e un concetto univoco. Sostanzialmente dato un set di elementi, indipendentemente da come si sceglie di descriverli in termini di feature, non c’`e un modo univoco di raggrupparli, perci`o
si tratta di un problema soggettivo.

Il clustering pu`o essere:
• Gerarchico, bottom-up
gerarchico a due a due (quindi si costruisce un albero di merge o di split).
• Partizionale, top-down
Il clustering gerarchico `e un tipo di clustering iterativo in cui si uniscono o si separano gli elementi in modo
Il clustering partizionale divide tutto il set di elementi in gruppi (partizioni).