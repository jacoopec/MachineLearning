import math 
import  numpy as np


X = np.array([[0.1, 0.7, 0.4, 0.2, 0.5, 0.8, 0.3],
            [0.12, 0.74, 0.64, 0.29, 0.65, 0.48, 0.23],
            [0.91, 0.47, 0.44, 0.12, 0.65, 0.84, 0.36],
            [0.22, 0.3, 0.45, 0.22, 0.1, 0.9, 0.43],
            [0.11, 0.37, 0.44, 0.92, 0.65, 0.38, 0.43]])

w = np.array([2.4, 1.9, 2.0, 0.45, 2.1, 0.9, 0.2])
b = np.array([0.4, 0.39, 0.40, 0.45, 0.1, 0.9, 0.2])

Y = np.array([0, 1, 1, 0, 1])


xp   = np.exp([w * x for x in X] + b)
P_Y0 = xp / (np.ones(len(w)) + xp)
P_Y1 = 1 / 1 + xp

print(P_Y0)
print(P_Y1)

# print(np.ones(len(w)))